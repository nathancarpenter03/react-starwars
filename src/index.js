import React from 'react';
import ReactDOM from 'react-dom';
// import App from './App';
import App2 from './App2';

// Props to be passed to our App component, txt set to a string, and cat set to #
// ReactDOM.render(
//     <App txt="this is the prop text" cat={5} />, document.getElementById('root')
// );

ReactDOM.render(
    <App2 />, document.getElementById('root')
);
